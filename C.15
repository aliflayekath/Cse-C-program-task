#include <stdio.h>

int main()
{
    int n, i;
    int p[101], a[101];

    scanf("%d", &n);

    for(i = 1; i <= n; i++)
    {
        scanf("%d", &p[i]);
        a[p[i]] = i;
    }

    for(i = 1; i <= n; i++)
        printf("%d ", a[i]);

    return 0;
}
