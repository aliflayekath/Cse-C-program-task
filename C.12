#include <stdio.h>

int main()
{
    int n, i, x;
    float sum = 0;

    scanf("%d", &n);

    for(i = 0; i < n; i++)
    {
        scanf("%d", &x);
        sum = sum + x;
    }

    printf("%f", sum / n);

    return 0;
}
